# API Schemas

See `src/api/schemas/` for Pydantic models.

## Key Types
- `SiteCreate` — site creation payload
- `SimulationRequest` — simulation configuration
- `SimulationResult` — simulation output
- `DiagnosticsResult` — anomaly detection output
